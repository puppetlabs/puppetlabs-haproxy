<% Array(@ipaddresses).zip(Array(@server_names)).each do |ipaddress,host| -%>
<% Array(@ports).each do |ports| -%>
  server <%= host %> <%= ipaddress %>:<%= ports %> <%= if @define_cookies then "cookie " + host end %> <%= Array(@options).join(" ") %>
<% end -%>
<% end -%>
